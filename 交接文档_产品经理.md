# 佛学智能问答系统 — 产品交接文档

> 交接对象：新任产品经理
> 文档日期：2026-06-09
> 当前分支：`claude/expand-buddhist-knowledge-base-BmIBm`

---

## 一、产品是什么（一句话）

这是一个**佛学知识智能问答系统**，用户用自然语言提问（如"什么是菩提心""如何开始禅修"），系统从佛教经论讲记中检索内容，再用大语言模型（LLM）组织成结构化的中文回答。当前知识库以《入菩萨行论》（入行论）为核心。

面向最终用户的是一个**类 ChatGPT 的聊天网页**，面向运营的是一套**后台管理系统**。

---

## 二、核心能力一览

| 能力 | 说明 | 面向 |
|------|------|------|
| 智能问答 | 语义检索 + LLM 生成结构化回答 | 用户 |
| 流式打字机效果 | 回答逐字浮现，类似 ChatGPT | 用户 |
| 查看讲记原文 | 回答下方可展开查看引用的原始讲记内容 | 用户 |
| 问题澄清 | 问题模糊时反问用户，引导明确意图 | 用户 |
| 点赞/点踩反馈 | 用户对回答质量打分，沉淀改进数据 | 用户 |
| 知识库导入 | 网页导入 / 文件导入 / 文本导入，可选 LLM 自动整理 | 运营 |
| 索引重建 | 导入内容后重建检索索引使其生效 | 运营 |
| 同义词管理 | 系统自动学习 + 人工审核词汇映射 | 运营 |
| 数据库实时查询（新） | 接入 PostgreSQL，回答"有多少人在学"等数据类问题 | 用户/运营 |

---

## 三、技术架构（产品视角，不需懂代码）

### 问答流程

```
用户提问
   │
   ├─ 安全检测（自杀/轻生等敏感词 → 心理援助回复）
   │
   ├─ 问题分类 → 判断这是哪类问题（定义/方法/原因/对比…共 32 类）
   │
   ├─ 数据查询判断 → 若是"有多少人""统计"类 → 走【SQL 数据库】路径
   │
   ├─ FAQ 精确匹配 → 命中常见问题库则直接返回
   │
   ├─ 混合检索 → 向量检索(FAISS) + 关键词检索(BM25) 并行
   │              从讲记中找出最相关的内容片段
   │
   ├─ LLM 生成 → 把检索到的内容交给大模型，组织成结构化回答
   │
   └─ 后处理 → 清理科判标记(甲乙丙丁)、回向文等，保证回答干净
```

### 两套"数据库"的区别（常被问到）

系统里有两种数据存储，**它们互补，不是替代关系**：

| | 知识库（默认） | SQL 数据库（可选接入） |
|---|---|---|
| 存什么 | 佛法讲记、经论原文（大段文字） | 用户、修行记录、统计数据（表格） |
| 格式 | `.txt` 文本文件 | PostgreSQL 数据表 |
| 怎么查 | 语义搜索（"意思接近"就能找到） | 精确查询（条件匹配、计数） |
| 适合 | "菩提心是什么" | "有多少人在学入行论" |

系统会**自动判断**问题类型走对应路径，运营不需要干预。

---

## 四、关键文件地图（交给技术同学时用）

| 文件 | 作用 |
|------|------|
| `api_server.py` | 后端服务总入口，97 个 API 端点（12 个公开 + 85 个后台） |
| `rag_answer.py` | 问答核心管线，所有回答逻辑都在这里 |
| `search_utils.py` | 检索逻辑（向量 + 关键词混合搜索、分块） |
| `build_faiss.py` | 索引构建（把 .txt 转成可检索的向量索引） |
| `question_classifier.py` | 32 类问题分类规则 |
| `query_rewrite.py` | 问题改写/扩写（提升检索命中率） |
| `import_knowledge.py` | 知识库导入与 LLM 自动整理 |
| `sql_bridge.py` | **新增** PostgreSQL 实时查询模块 |
| `llm_client.py` | 大模型连接配置（支持多家供应商） |
| `rag_runtime_config.py` | 所有可调参数的总配置文件 |
| `web/chat.html` | 用户端聊天页面 |
| `admin_page.html` | 后台管理页面 |
| `knowledge/buddhism/` | 知识库内容目录（入行论各品 .txt） |

---

## 五、运营需要知道的操作

### 1. 如何新增/更新知识内容
1. 后台 → 导入（支持网页链接、上传文件、直接粘贴文本三种方式）
2. 可勾选"LLM 自动整理"——让大模型把原始文稿清洗成规范格式
3. 导入后**必须点击"重建索引"**，新内容才会生效（这一步耗时较长，大库可达数十分钟）

### 2. 如何提升回答质量
- 后台可查看用户点踩的回答，针对性补充知识或调整
- 同义词管理：系统会自动学习用户用词（如"业障"→"业力"），高频映射自动审核，也可人工添加

### 3. 启动服务的关键环境变量
```
RAG_USE_OPENAI=1        # 必须设为 1，否则不调用 LLM，回答质量大幅下降
ADMIN_API_KEY=xxx       # 后台管理密钥
ASK_TIMEOUT_SEC=120     # 单次问答超时（秒）
```

---

## 六、当前状态与已知情况

### 已完成
- 入行论全 10 品知识库已导入
- 问答质量自动化测试 174 个用例，通过率约 98%
- 类 ChatGPT 流式打字机效果
- "查看讲记原文"功能
- PostgreSQL 实时查询框架（代码已就绪）

### 待办 / 需要决策
1. **SQL 数据库尚未真正接入** — 框架已写好，但需要：
   - 安装 `psycopg2-binary` 依赖
   - 在 `data/sql_config.json` 填入真实的 PostgreSQL 连接信息和表结构
   - 把 `enabled` 设为 `true` 并重启
   - **需要新 PM 提供另一个项目的数据库表结构（CREATE TABLE 语句）**
2. 多位法师讲记的扩展（目前主要是索达吉堪布、生西法师两套）
3. GPU 显存限制：当前配置针对 6GB 显存优化（批次大小调小），若换更大显卡可调高提升建索引速度

### 注意事项
- 知识库回答必须开启 LLM（`RAG_USE_OPENAI=1`），否则只走规则提取，回答会变成原文片段堆砌
- 回答中不应出现科判标记（甲一、乙二等）、回向文——已有后处理清理，若发现遗漏需补充清理规则

---

## 七、相关历史文档（仓库内已有，可深入阅读）

| 文档 | 内容 |
|------|------|
| `01_当前版本完整框架结构.md` | 系统框架结构详解 |
| `02_产品功能文档.md` | 产品功能清单 |
| `03_所有接口详细作用.md` | 全部 API 接口说明 |
| `04_版本更新详细说明.md` | 版本更新记录 |
| `05_替换与使用说明.md` | 部署与替换说明 |
| `ARCHITECTURE.md` | 技术架构 |
| `CHANGELOG.md` | 变更日志 |
| `docs/知识库格式规范.md` | 知识库文件格式规范 |

---

## 八、交接联系要点

- **代码仓库**：`ericjepsen9/rag-test-buddhism`
- **开发分支**：`claude/expand-buddhist-knowledge-base-BmIBm`
- **核心待办**：接入 PostgreSQL（需数据库表结构）、持续扩充知识库、根据用户反馈优化回答质量
