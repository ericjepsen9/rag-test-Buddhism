# 佛学智能问答系统 — 产品交接文档（详细版）

> 交接对象：新任产品经理
> 文档日期：2026-06-09
> 代码仓库：`ericjepsen9/rag-test-buddhism`
> 开发分支：`claude/expand-buddhist-knowledge-base-BmIBm`
> 代码规模：约 18,900 行 Python（25 个模块）+ 4 个前端页面

---

## 第一部分：产品概述

### 1.1 一句话定位

一个**佛学知识智能问答系统（RAG）**：用户用自然语言提问，系统从佛教经论讲记中检索相关内容，再用大语言模型组织成结构化的中文回答。当前知识库以《入菩萨行论》（入行论）为核心，10 品内容完整。

### 1.2 RAG 是什么（给非技术 PM）

RAG = Retrieval-Augmented Generation（检索增强生成）。不是让 AI 凭记忆瞎答，而是：
1. **先检索** —— 从我们自己的佛法讲记库里找到最相关的原文片段
2. **再生成** —— 把这些片段交给大模型，让它"基于这些材料"组织回答

好处：回答有出处、不编造、可溯源（用户能点开"查看讲记原文"核对）。

### 1.3 三类用户与三个入口

| 用户 | 入口 | 页面文件 |
|------|------|---------|
| 终端用户（信众/学员） | PC 聊天页 | `web/chat.html` |
| 终端用户（手机） | 移动端页 | `mobile.html` |
| 运营/管理员 | 后台管理系统 | `admin_page.html` |

---

## 第二部分：功能清单（详细）

### 2.1 用户端问答功能

| 功能 | 详细说明 |
|------|---------|
| **自然语言问答** | 支持定义、方法、原因、对比、辩论等各类提问 |
| **两种回答模式** | `brief`（简洁）/ `full`（详细），用户可切换 |
| **两种用户级别** | `beginner`（初学者，多解释）/ `experienced`（有基础，更精炼），影响回答深度与术语使用 |
| **流式打字机效果** | 回答逐字浮现，类 ChatGPT 体验；短回答（<20字）直接显示 |
| **查看讲记原文** | 回答下方可展开面板，显示本次引用的原始讲记片段及其科判出处，最多 5 个来源 |
| **问题澄清** | 问题过短或模糊时（6–15 字且歧义），系统反问并给出候选选项引导 |
| **多轮对话** | 保留最近对话历史，支持"详细说说""展开讲"等追问 |
| **点赞/点踩反馈** | 用户对每条回答打分（👍/👎），数据写入反馈日志用于优化 |
| **安全干预** | 检测自杀/轻生等敏感词，返回心理援助热线 + 佛法慰藉，不走常规问答 |
| **相关媒体** | 回答可附带相关图片/视频链接（基于 media.json 映射） |
| **关联信息** | 回答可附带跨实体关联（如人物、经典、概念之间的关系） |
| **复制回答** | 一键复制回答文本 |
| **PWA 离线支持** | 含 `manifest.json` + `sw.js`，可"添加到主屏幕"，弱网友好 |

### 2.2 问答智能处理（后台自动，用户无感）

**32 类问题分类**（`question_classifier.py`，纯规则、零延迟、零成本）：

> definition（定义）、method（方法）、reason（原因）、comparison（对比）、debate（辩论）、list（列举）、overview（概述）、who（人物）、which（归属）、verse（颂词）、original_text（原文）、howmany（数量）、reference（出处）、relation（关系）、sequence（次第）、story（公案）、chanting（念诵）、advice（建议）、practice_detail（修行细节）、difficulty（修行困难）、experience（觉受）、precept（戒律）、application（生活应用）、emotion（情绪）、taboo（禁忌）、misconception（误解）、contemporary（现代议题）、verification（求证）、followup（追问）、recommendation（推荐）、etiquette（礼仪）、calendar（节日历法）、meta（关于系统本身）

每类问题驱动不同的搜索策略（是否跳过 FAQ、是否优先讲记内容、是否拆分检索等）。

**10 类内容路由**（`QUESTION_ROUTES`）：
> doctrine（教义）、practice（修行）、scripture（经典）、sect（宗派）、concept（概念）、history（历史）、ritual（仪轨）、basic（基础）、life（生活应用）、buddhism（通用）

系统对每个问题打分判断归属路由，不同路由用不同的检索权重和阈值。

**问题改写（Query Rewrite）**：扩写、同义词补充、别名召回，提升检索命中率；带 LLM 改写缓存。

### 2.3 检索引擎（混合检索）

| 检索方式 | 说明 |
|---------|------|
| **向量检索（FAISS）** | 用 BGE-M3 模型把文字转向量，按语义相似度搜索，默认权重 0.65 |
| **关键词检索（BM25）** | 基于 jieba 中文分词的关键词匹配，默认权重 0.35 |
| **混合融合** | 两路结果按路由权重融合排序 |
| **CrossEncoder 精排** | 用 BGE-reranker-v2-m3 对候选做二次精排（可开关） |
| **分数过滤 + 去重** | 默认阈值 0.25，过滤低相关、去除重复片段 |
| **内容分层过滤** | 按内容类型（教义/修行/生活）调整权重，避免生活类内容抢占教义问答 |

### 2.4 回答质量保障

- **后处理清理**（`_clean_llm_output`，22 条规则）：自动去除科判标记（甲一、乙二、丙三…）、【讲解】等标签、回向文、原始排版残留，保证回答干净
- **上下文预处理**（`_build_context`）：检索片段送入 LLM 前先清洗去重
- **回声检测**：防止 LLM 把问题原样复述当回答
- **置信度检测**：低置信度时附加提示，避免误导
- **自动化测试**：174 个测试用例，25 个分类，当前通过率约 98%

### 2.5 运营端：知识库管理

| 功能 | 说明 |
|------|------|
| **三种导入方式** | 网页导入（URL 抓取）、文件导入（上传）、文本导入（粘贴） |
| **支持文件格式** | `.txt` `.md` `.doc` `.docx` `.pdf` `.html` `.zip`（批量） |
| **LLM 自动整理** | 可选，让大模型把原始文稿清洗成规范的结构化知识库格式 |
| **三步导入流程** | 抽取文本 → LLM 精炼预览 → 确认入库 |
| **网站爬取（crawl_v2）** | 自动发现站点链接、分页抓取、断点续传、暂停/恢复/重试、SSE 实时进度 |
| **乱码处理** | 针对 `.doc` 多编码自动识别（olefile 解析） |
| **产品/知识管理** | 创建产品、增删改查知识文件、删除产品 |
| **索引重建** | 导入后重建 FAISS 索引使内容生效（耗时操作，超时上限 3600 秒） |

### 2.6 运营端：词库与配置管理

| 功能 | 说明 |
|------|------|
| **同义词管理** | 系统自动学习用户用词映射（如"业障"→"业力"），出现≥3 次自动审核，也支持人工添加/编辑/批量审核/批量删除 |
| **关键词管理** | 静态同义词覆盖、消歧规则、LLM 扩充关键词、从现有库提取关键词 |
| **参数调优** | 后台可调 BM25 参数、混合检索权重、Top-K、阈值、Reranker 开关等约 20 项 |
| **LLM 配置** | 对话用 / 知识整理用 两套独立配置，支持多供应商（OpenAI/DeepSeek/MiniMax/自定义），含连接测试 |
| **服务管理** | 嵌入模型、LLM 服务的启停与状态查看 |
| **缓存管理** | 查看/清理响应缓存、检索缓存 |
| **诊断工具** | `/admin/diagnose` 单步调试整条问答管线 |
| **日志查看** | QA 日志、未命中日志、错误日志、系统日志 |
| **统计面板** | 系统状态、调用统计 |

### 2.7 数据库实时查询（新增，框架就绪待接入）

新增 `sql_bridge.py` 模块，支持接入 **PostgreSQL** 回答数据类问题：
- 自动判断"有多少人""统计""排行"等数据查询类问题
- LLM 将自然语言转 SQL（Text-to-SQL）
- 安全校验（仅允许 SELECT，禁止增删改、禁止注释、禁止危险函数）
- 连接池、查询超时、行数上限保护
- 查询结果再经 LLM 转为自然语言回答
- 后台接口：配置、连接测试、读取表结构、手动查询、自然语言测试查询

---

## 第三部分：技术栈

### 3.1 后端

| 层 | 技术 | 说明 |
|----|------|------|
| Web 框架 | **FastAPI** + Uvicorn | Python 异步 Web 服务 |
| 数据校验 | Pydantic v2 | 请求/响应模型 |
| 限流 | slowapi | 基于 IP 的速率限制 |
| 上传 | python-multipart | 文件上传 |

### 3.2 AI / 检索

| 组件 | 技术 | 说明 |
|------|------|------|
| 嵌入模型 | **BAAI/bge-m3**（FlagEmbedding） | 中文向量化，支持 FP16 |
| 向量库 | **FAISS**（GPU 版） | 向量索引，支持 flat / HNSW |
| 精排模型 | BAAI/bge-reranker-v2-m3 | CrossEncoder 二次精排 |
| 关键词检索 | **BM25** + jieba | 中文分词关键词匹配 |
| 深度学习 | PyTorch ≥ 2.0 | 模型运行时 |
| 大模型 | **OpenAI 兼容接口** | 支持 OpenAI / DeepSeek / MiniMax / 自定义，可配 LiteLLM 格式 |

### 3.3 数据解析 / 抓取

| 用途 | 技术 |
|------|------|
| 网页抓取 | requests + BeautifulSoup4 |
| Word 文档 | python-docx + olefile（旧版 .doc） |
| PDF | （PDF 解析） |

### 3.4 前端

| 技术 | 说明 |
|------|------|
| 原生 HTML/CSS/JS | 无框架，单文件页面，轻量 |
| PWA | manifest.json + Service Worker，可安装、离线缓存 |
| SSE | Server-Sent Events，用于爬取进度等实时推送 |
| 兼容接口 | 提供 `/v1/chat/completions` OpenAI 兼容端点，可被第三方客户端接入 |

### 3.5 数据存储

| 数据 | 存储方式 |
|------|---------|
| 佛法知识内容 | `.txt` 文本文件（`knowledge/buddhism/`） |
| 向量索引 | `stores/<product>/index.faiss` + `docs.jsonl` |
| 同义词/关键词/配置 | `data/*.json` 文件 |
| 日志 | 文件日志（QA / miss / error / system） |
| 外部业务数据 | PostgreSQL（可选接入，sql_bridge） |

---

## 第四部分：系统架构

### 4.1 问答主流程

```
用户提问
   │
   ├─ ① 安全检测 ──────── 命中敏感词 → 心理援助回复（终止）
   │
   ├─ ② 问题分类 ──────── 判定 32 类问题之一 → 决定搜索策略
   │
   ├─ ③ SQL 路由判断 ──── 数据查询类 → Text-to-SQL → 查 PostgreSQL → LLM 组织回答（终止）
   │
   ├─ ④ FAQ 精确匹配 ──── 命中常见问题 → 直接返回（可经 LLM 调风格）
   │
   ├─ ⑤ 问题改写 ──────── 扩写/同义词/别名召回
   │
   ├─ ⑥ 混合检索（并行）── 向量(FAISS) + 关键词(BM25) → 融合排序 → 过滤去重
   │
   ├─ ⑦ CrossEncoder 精排（可选）
   │
   ├─ ⑧ LLM 生成 ──────── 检索片段 + 问题类型提示 → 结构化回答
   │
   ├─ ⑨ 关联/媒体增强 ─── 附加关联信息、相关图片视频
   │
   └─ ⑩ 后处理清理 ────── 去科判/标签/回向文 → 返回干净回答

   兜底：LLM 无法生成时 → 规则抽取；仍无 → 未命中回复并记录知识缺口
```

### 4.2 两套数据存储的区别（高频被问）

| | 知识库（默认） | SQL 数据库（可选接入） |
|---|---|---|
| 存什么 | 佛法讲记、经论原文（大段非结构化文字） | 用户、修行记录、统计数据（结构化表格） |
| 格式 | `.txt` 文本文件 + FAISS 向量索引 | PostgreSQL 数据表 |
| 怎么查 | 语义搜索（意思接近就能找到） | 精确查询（条件匹配、计数、聚合） |
| 擅长 | "菩提心是什么""如何禅修" | "有多少人在学入行论""本月签到排行" |
| 不擅长 | 精确统计 | 模糊语义理解 |

**两者互补，系统自动判断走哪条路径，运营无需干预。**

---

## 第五部分：API 接口总览

共 **97 个端点**：12 个公开 + 85 个后台管理。

### 5.1 公开端点（终端用户）

| 方法 | 路径 | 作用 |
|------|------|------|
| POST | `/ask` | **核心问答接口** |
| GET | `/chat` | PC 聊天页 |
| GET | `/mobile` | 移动端页 |
| GET | `/health` | 健康检查 |
| GET | `/sw.js` | Service Worker（PWA） |
| POST | `/v1/chat/completions` | OpenAI 兼容问答（支持流式） |
| GET | `/v1/models` | OpenAI 兼容模型列表 |
| POST | `/admin/feedback` | 用户点赞/点踩反馈 |

### 5.2 后台管理端点（按功能分组）

| 分组 | 代表端点 | 数量 |
|------|---------|------|
| 知识库导入 | `/admin/import_knowledge`、`/admin/upload`、`/admin/fetch_url`、`/admin/batch_pattern_import` | ~12 |
| 网站爬取 v2 | `/admin/crawl_v2/discover`、`/start`、`/jobs/{id}/pause|resume|retry|stop` | ~11 |
| 知识文件管理 | `/admin/knowledge/{product}/{filename}`（增删改查）、`/admin/rebuild` | ~8 |
| 同义词管理 | `/admin/synonyms/*`（增删改查、审核、导入导出） | ~11 |
| 关键词管理 | `/admin/keywords/*`（覆盖、消歧、LLM 扩充） | ~9 |
| 参数/服务配置 | `/admin/config`、`/admin/llm/configs`、`/admin/service/*` | ~14 |
| **SQL 数据库** | `/admin/sql/config|test|schema|query|ask` | 6 |
| 缓存/统计/日志 | `/admin/cache`、`/admin/stats`、`/admin/logs/*`、`/admin/diagnose` | ~10 |

> 完整接口清单见仓库 `03_所有接口详细作用.md`。

---

## 第六部分：关键文件地图

| 文件 | 行数 | 作用 |
|------|------|------|
| `api_server.py` | 4656 | 后端服务总入口，97 个 API 端点 |
| `rag_answer.py` | 2182 | **问答核心管线**，所有回答逻辑 |
| `search_utils.py` | 1921 | 检索逻辑（混合搜索、分块、内容类型识别） |
| `import_knowledge.py` | 1075 | 知识库导入与 LLM 整理 |
| `build_faiss.py` | 925 | FAISS 索引构建 |
| `rag_runtime_config.py` | 907 | **全部可调参数总配置** |
| `query_rewrite.py` | 745 | 问题改写/扩写 |
| `keyword_extractor.py` | 622 | LLM 辅助关键词提取 |
| `crawl_v2.py` | 544 | 网站爬取引擎 |
| `sql_bridge.py` | 469 | **PostgreSQL 实时查询（新增）** |
| `clarification_engine.py` | 439 | 问题澄清引导 |
| `keyword_store.py` | 421 | 关键词存储管理 |
| `llm_client.py` | 377 | 多供应商 LLM 连接 |
| `rag_logger.py` | 281 | 日志（qa/error/event） |
| `relation_engine.py` | 247 | 跨实体关联引擎 |
| `synonym_store.py` | 239 | 同义词持久化与自学习 |
| `question_classifier.py` | 152 | 32 类问题分类 |
| `media_router.py` | — | 媒体关联 |
| `answer_formatter.py` | — | 回答结构化格式化 |
| `web/chat.html` | — | 用户端聊天页 |
| `admin_page.html` | — | 后台管理页 |
| `knowledge/buddhism/` | — | 知识库内容（入行论 10 品 .txt） |
| `test_quality.py` | 526 | 174 用例质量测试 |

---

## 第七部分：关键配置参数

> 全部在 `rag_runtime_config.py`，多数支持环境变量覆盖。

### 7.1 启动必备环境变量

```bash
RAG_USE_OPENAI=1        # 必须为 1，否则不调用 LLM，回答质量大幅下降
ADMIN_API_KEY=xxx       # 后台管理密钥
ASK_TIMEOUT_SEC=120     # 单次问答超时（秒）
```

### 7.2 核心可调参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `EMBED_MODEL_NAME` | BAAI/bge-m3 | 嵌入模型 |
| `EMBED_BATCH_SIZE_BUILD` | 2 | 建索引批次（针对 6GB 显存调低，大显卡可提高加速） |
| `CHUNK_SIZE` / `CHUNK_OVERLAP` | 600 / 150 | 文本分块大小/重叠 |
| `DEFAULT_TOP_K` | 8 | 默认检索返回数 |
| `HYBRID_VECTOR_WEIGHT` / `KEYWORD_WEIGHT` | 0.65 / 0.35 | 混合检索权重 |
| `SCORE_THRESHOLD` | 0.25 | 相关性过滤阈值 |
| `BM25_K1` / `BM25_B` | 1.5 / 0.75 | BM25 调参 |
| `RERANK_MODEL` | bge-reranker-v2-m3 | 精排模型 |
| `LLM_MAX_TOKENS_BRIEF/FULL` | 800 / 1500 | 简洁/详细回答长度上限 |
| `LLM_TEMPERATURE` | 0.3 | 生成温度（越低越稳定） |
| `FAISS_INDEX_TYPE` | flat | 索引类型（flat/hnsw） |

---

## 第八部分：当前状态与待办

### 8.1 已完成

- ✅ 入行论全 10 品知识库导入
- ✅ 问答质量自动化测试 174 用例，通过率约 98%
- ✅ 类 ChatGPT 流式打字机效果（已修复空白回答 bug）
- ✅ "查看讲记原文"功能（已修复按钮无响应 bug）
- ✅ 用户反馈（点赞/点踩）
- ✅ PWA 离线支持
- ✅ 32 类问题分类 + 10 类路由 + 混合检索 + 精排
- ✅ 完整后台管理（导入/爬取/词库/参数/日志/诊断）
- ✅ PostgreSQL 实时查询框架（代码就绪）

### 8.2 待办 / 需新 PM 决策

1. **🔴 SQL 数据库接入（最高优先）** —— 框架已就绪，但需要：
   - 安装依赖：`pip install psycopg2-binary`
   - 在 `data/sql_config.json` 填入真实 PostgreSQL 连接信息 + 表结构
   - 设 `enabled: true` 并重启
   - **需提供另一个项目的数据库表结构（CREATE TABLE 语句），并为每个字段写中文描述**（描述质量直接决定 Text-to-SQL 准确率）
2. **多法师讲记扩展** —— 目前以索达吉堪布、生西法师两套为主，可扩充更多讲记/经论
3. **硬件升级** —— 当前针对 6GB 显存优化（批次调小拖慢建索引），换大显卡可提速
4. **持续质量优化** —— 根据用户点踩反馈与未命中日志补充知识、调参

### 8.3 注意事项（踩坑提醒）

- ⚠️ 知识库回答**必须开启 LLM**（`RAG_USE_OPENAI=1`），否则退化为原文片段堆砌
- ⚠️ 导入内容后**必须重建索引**才生效，大库耗时可达数十分钟
- ⚠️ 回答中若出现科判标记（甲乙丙丁）、回向文，需补充 `_clean_llm_output` 清理规则
- ⚠️ SQL 路径仅允许 SELECT，已做安全校验，但接入生产库建议用**只读账号**双重保险

---

## 第九部分：仓库内其他文档索引

| 文档 | 内容 |
|------|------|
| `01_当前版本完整框架结构.md` | 框架结构详解 |
| `02_产品功能文档.md` | 功能清单 |
| `03_所有接口详细作用.md` | 全部 API 接口详解 |
| `04_版本更新详细说明.md` | 版本更新记录 |
| `05_替换与使用说明.md` | 部署与替换说明 |
| `ARCHITECTURE.md` | 技术架构 |
| `CHANGELOG.md` | 变更日志 |
| `PROGRESS.md` | 进度记录 |
| `docs/知识库格式规范.md` | 知识库文件格式规范 |
| `交接文档_产品经理.md` | 交接文档（精简版） |
| `交接文档_产品经理_详细版.md` | 本文档（详细版） |

---

## 附：交接联系要点

- **代码仓库**：`ericjepsen9/rag-test-buddhism`
- **开发分支**：`claude/expand-buddhist-knowledge-base-BmIBm`
- **首要任务**：接入 PostgreSQL（需数据库表结构 + 字段中文描述）
- **核心心法**：这是"检索 + 生成"系统——内容质量（知识库）决定回答上限，参数调优决定下限，LLM 必须开启
